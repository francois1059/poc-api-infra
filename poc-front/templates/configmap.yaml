apiVersion: v1
kind: ConfigMap
metadata:
  name: front-config
data:
  config.js: |
    var config = (() => {
      return {
        "VITE_API_BASE_URL": "http://todos-api.local",
        "VITE_API_TODOS_PATH": "/todos",
      };
    })();